#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <vector>
#include <utility>

std::vector<int> num_oculto();
std::vector<int> intento(int num);
std::pair<int, int> comprobacion(const std::vector<int>& num, const std::vector<int>& numIngresado);

#endif // FUNCTIONS_H
